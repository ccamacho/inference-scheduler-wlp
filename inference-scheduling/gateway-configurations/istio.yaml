gateway:
  gatewayClassName: istio
  service:
    type: LoadBalancer  # Default as per getting-started-inferencing.md
  destinationRule:
    enabled: true
    trafficPolicy:
      tls:
        mode: SIMPLE
        insecureSkipVerify: true

provider:
  name: istio 